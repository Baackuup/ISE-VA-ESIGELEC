void init_display (void); void Init_motors(void); and uint8_t compass_init(void); 
The functions are not written in the same way - some start with a capital letter, others do not. One function starts with uint.
These differences are explained by the signature and type of the return value.